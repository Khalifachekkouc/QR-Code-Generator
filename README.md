# QR-Code-Generator
📲 QR Code Generator

A simple yet powerful web application built with Vanilla JavaScript that allows users to instantly generate QR codes from any text or URL. It uses a clean, paper-inspired UI and integrates an external API for high-quality QR code generation.

🚀 Features

Instant QR Generation: Convert any text, link, or URL into a scanable QR code in seconds.

Loading States: Includes a "Generating..." state with a 1-second delay to simulate a realistic processing experience.

Smooth UI Transitions: The interface expands dynamically to show the generated QR code once it's ready.

Smart Input Handling: Automatically hides the QR code section when the input field is cleared.

Responsive Design: Works perfectly on mobile and desktop browsers.

🛠️ Tech Stack

HTML5 & CSS3: For the card-based layout and smooth animations.

JavaScript (ES6+): Handles user input, API requests, and DOM updates.

QR Server API: Used to generate the QR code images dynamically.
